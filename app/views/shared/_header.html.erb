<nav class="navbar navbar-default" ng-controller="HeaderController as ctrl">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">JetSetGo</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li ng-hide="ctrl.currentUser"><a ng-click="ctrl.openSignInModal()" href="">Sign in</a></li>
        <li ng-hide="ctrl.currentUser"><a ng-click="ctrl.openSignUpModal()" href="">Register</a></li>
        <li ng-show="ctrl.currentUser"><a ng-click="ctrl.signOut()" href="">Sign Out</a></li>
      </ul>
    </div>
  </div>
</nav>